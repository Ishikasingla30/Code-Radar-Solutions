#include <stdio.h>
int main() {
    int a;
    scanf("%d", &a && year % 100 != 0) || (year % 400 == 0));
    if(a%4==0) {
        printf("Leap Year");
    }
    else {
        printf("Not a Leap Year");
    }
    return 0;
}