#include <stdio.h>
int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    if(x>=1 && x<=100) {
        printf("In Range");
    }
    else {
        printf("Out of Range");
    }
    return 0;
}