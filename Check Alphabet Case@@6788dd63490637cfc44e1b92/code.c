#include<stdio.h>
int main() {
    char alphabet;
    scanf("%c, &alpabet");
    if(alphabet == 'A') {
        printf("Uppercase");
    }
    ifelse(alphabet == 'a') {
        printf("Lowercase");
    }
    return 0;
}