#include<stdio.h>
int main() {
    char alphabet;
    scanf("%c, &alpabet");
    if(alphabet == 'A') {
        printf("Uppercase");
    }
    else {
        printf("Lowercase");
    }
    return 0;
}