#include <stdio.h>
#include <stdio.h>

int main() {
    double num;

    // Taking double input
    scanf("%lf", &num);

    // Printing the double with required format
    printf("You entered: %.4lf\n", num);

    return 0;
}
